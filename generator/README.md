# GENERATOR

This module generate PUSIT cards.
Which means that RFID cards that is not generated by this GENERATOR, is an invalid PUSIT card.
Hence, they cannot use it to interact with the PUSIT system.

Unlike the READER, I chose it to be wired so that it is alongside the server

## Components
- Arduino Nano
- W5500 Lan Module
- RFID RC522
